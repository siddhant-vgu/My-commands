import webbrowser

#this is new code that opens the Claude AI website in the default web browser using Python's webbrowser module. It prints a message indicating that it is opening Claude via the Python CLI and then uses the webbrowser.open() function to navigate to the specified URL.
print("Opening Claude via Python CLI...")
webbrowser.open("https://claude.ai/new")
